{
  "name": "influx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./dist/",
    "build": "npm run clean && ./node_modules/.bin/tsc --build tsconfig.build.json",
    "postbuild": "cp package*.json dist && cd dist && npm ci --production",
    "dev": "ts-node -r dotenv/config src/index.ts",
    "read": "ts-node -r dotenv/config src/xgtClient/snippets/readSample.ts",
    "write": "ts-node -r dotenv/config src/xgtClient/snippets/writeSample.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@influxdata/influxdb-client": "^1.33.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "rimraf": "^4.4.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}